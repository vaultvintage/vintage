# Base URL of the Django REST API (no trailing slash).
# The frontend appends /api/v1/... to this value.
# Production (set in Vercel env): https://api.vintagefort.com
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000
